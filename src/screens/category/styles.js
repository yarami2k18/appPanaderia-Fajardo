import { StyleSheet, Dimensions } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    Image: {
        width: '80%',
        height: '40%',
    },
    cardContainer: {
        width: '40%',
        height: '40%',
    }
})